1. Differentiate between abstract classes and concrete classes in Python. Write a program to create an abstract class `Vehicle` with an abstract method `max_speed()` and implement it in two concrete classes `Car` and `Bike`. [2+4 marks]
2. Explain how Python handles multiple inheritance and method resolution order (MRO) with a suitable example. [5 marks]
3. What is method overriding? Demonstrate with a Python program how a child class can override a parent's method while still using `super()` to call the original parent method. [6 marks]
4. Contrast composition and aggregation in object-oriented programming. Illustrate the difference in lifetime dependency using a Python example with classes `Department` and `Employee`. [6 marks]
5. Explain the concept of encapsulation. How can you define private attributes in Python? Write a program implementing a class `BankAccount` with private attributes and use property decorators (getter and setter methods) to access and modify them. [6 marks]
6. What is broadcasting in NumPy? Explain its rules and provide a program demonstrating how a 1D array is added to a 2D array. [5 marks]
7. Explain the difference between shallow copy (view) and deep copy in NumPy arrays with a suitable program showing that modifying a deep copy does not affect the original array. [5 marks]
8. Write a Python program to solve the following system of linear equations using the NumPy library: `3x + 4y = 18` and `2x - y = 4`. [5 marks]
9. Discuss the difference between a Python module, package, and library. Create a custom module `stringutils.py` containing a function `is_palindrome()` and write another script to import and use it. [2+4 marks]
10. Write a program using NumPy to create an array of 30 random integers between 10 and 50. Find the mean, median, standard deviation, and variance of this array using NumPy built-in statistics functions. [6 marks]
